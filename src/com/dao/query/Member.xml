<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Member">
		<insert id="addMember" parameterClass="com.dto.MemberDTO">
		insert into
		member(memberId,title,firstName,middleName,lastName,mobile,email,address,place,mandal,district,state,pincode,profession,updatedBy,updatedOn)
		values(#memberId#,#title#,#firstName#,#middleName#,#lastName#,#mobile#,#email#,#address#,#place#,#mandal#,#district#,#state#,#pincode#,#profession#,#updatedBy#,#updatedOn#)
	</insert>
	<select id="getMemberDetails" resultClass="com.dto.MemberDTO">
		select
		memberId,title,firstName,middleName,lastName,mobile,email,address,place,mandal,district,state,pincode,profession,updatedBy,updatedOn from
		member
	</select>
	<select id="getMemberById" parameterClass="java.lang.String"
		resultClass="com.dto.MemberDTO">
		select memberId,title,firstName,middleName,lastName,mobile,email,address,place,mandal,district,state,pincode,profession,updatedBy,updatedOn from
		member where memberId = #value#
	</select>
	<update id="memberUpdate" parameterClass="com.dto.MemberDTO">
		update member set title
		= #title#, firstName = #firstName#, middleName = #middleName#, lastName = #lastName#, mobile
		= #mobile#, email = #email#, address = #address#, place = #place#, mandal = #mandal#, district = #district#, state = #state#, pincode = #pincode#,
		profession = #profession#, updatedBy = #updatedBy#, updatedOn = #updatedOn#
		where
		memberId = #memberId#
	</update>
	<delete id="deleteMember" parameterClass="com.dto.MemberDTO">
		delete from member where
		memberId = #memberId#
	</delete>
</sqlMap>